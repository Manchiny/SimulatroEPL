using UnityEngine;

namespace SimulatorEPL.UI
{
    public class GameCoefsView : MonoBehaviour
    {
        private GameCoefs coefs;

        public void SetGameCoef(GameCoefs coefs)
        {
            this.coefs = coefs;
        }
    }
}
